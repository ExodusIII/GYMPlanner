# .NET
bin/
obj/
*.user

# Local SQLite dev database
*.db
*.db-shm
*.db-wal

# Node / Vite
web/node_modules/
web/dist/

# Secrets / local config
appsettings.*.local.json
.env.local
# Docker Compose env file at the repo root (may hold API keys). web/.env is safe and tracked.
/.env

# IDE
.vs/
.idea/
